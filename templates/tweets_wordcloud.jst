<% _.each(collection, function (frequency, word) { %>
  <a href="#<%= encodeURIComponent(model.get('screen_name')) %>/<%= encodeURIComponent(word) %>" rel="<%= frequency %>"><%= App.escapeHTML(word) %></a>
<% }); %>
